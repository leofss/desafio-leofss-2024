class Habitat {
    constructor(id, biome, totalSize, existingAnimals, remainingSpace) {
        this.id = id;
        this.biome = biome;
        this.totalSize = totalSize;
        this.existingAnimals = existingAnimals;
        this.remainingSpace = remainingSpace
    }
}

export { Habitat };
